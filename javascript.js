// Display an alert with the message "Hello, World!" once the page has loaded

const alertMessage = () => {
  alert("Hello, World!");
}

// Call function when the page has loaded
window.onload = () => {
  alertMessage();
}